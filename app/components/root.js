import React, { Component } from 'react';
import Layout from '../containers/layout';

class Root extends Component {
    render() {
        return (
            <Layout/>
        );
    }
}

export default Root;
